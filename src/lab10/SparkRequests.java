package lab10;

public class SparkRequests {
//    df_reader = spark.read.csv('hdfs://localhost:9000/library/reader_file.csv', header=True)
//    df_book = spark.read.csv('hdfs://localhost:9000/library/book_file.csv', header=True)
//
//            df_reader.createOrReplaceTempView("reader")
//            df_book.createOrReplaceTempView("book")
//    result = spark.sql("""SELECT DISTINCT r.id, r.source_fio, r.source_registration_date,
//						 r.source_address, r.source_email, b.source_output_overdue
//						 FROM reader AS r, book AS b
//						 WHERE b.source_output_overdue == True AND b.source_output_id_reader == r.id
//						 ORDER BY r.id""")
//            result.show()

}
